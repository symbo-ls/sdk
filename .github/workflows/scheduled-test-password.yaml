name: Scheduled Tests Workflow
on:
  repository_dispatch:
    types: [run_sdk_tests]
  workflow_dispatch:
  schedule:
    - cron: '0 8 * * *'

jobs:
  scheduled-tests:
    uses: ./.github/workflows/test-workflow.yaml
    with:
      node_env: 'testing'
      suite: 'password'
    secrets: inherit
